{
  "name": "jharsankalp",
  "version": "0.1.0",
  "private": true,
  "description": "JharSankalp — Societal Challenge-to-Impact Exchange for Jharkhand",
  "workspaces": [
    "shared",
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "npm run dev -w server",
    "dev:client": "npm run dev -w client",
    "build": "npm run build -w shared && npm run build -w client && node -e \"const fs = require('fs'); fs.cpSync('client/dist', 'dist', { recursive: true });\"",
    "build:client": "npm run build -w shared && npm run build -w client",
    "build:server": "npm run build -w shared && npm run build -w server",
    "build:all": "npm run build:server && npm run build:client",
    "build:shared": "npm run build -w shared",
    "start": "npm run start --workspace=@jharsankalp/server",
    "lint": "eslint . --ext .ts,.tsx",
    "format": "prettier --write \"**/*.{ts,tsx,json,css,md}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,json,css,md}\"",
    "clean": "rimraf node_modules server/node_modules client/node_modules shared/node_modules"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "concurrently": "^9.1.2",
    "prettier": "^3.5.3",
    "rimraf": "^6.0.1",
    "tsx": "^4.19.4"
  }
}
